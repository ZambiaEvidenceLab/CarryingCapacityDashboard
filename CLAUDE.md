## Agent skills

### Issue tracker

Issues and specs live as local markdown files under `.scratch/`. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout — `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.

## Git commits

Never run `git commit` in this repo. Stage the work if useful, then output the proposed commit message as text and stop — the user commits it themselves after reviewing.
